{
  "name": "@oraichain/perpeptual",
  "version": "1.0.0",
  "author": "Thanh Tu <tu@orai.io>",
  "license": "Apache-2.0",
  "dependencies": {
    "@cosmjs/cosmwasm-stargate": "^0.29.5",
    "@cosmjs/amino": "^0.29.5"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  },
  "scripts": {
    "build": "cw-build contracts/margined_* -s",
    "build:ts": "cw-gents contracts/margined_* -o build && git apply comments.patch",
    "build:docs": "typedoc --entryPointStrategy expand --name 'Margined Protocol' --readme doc/v1-design.md build"
  }
}
